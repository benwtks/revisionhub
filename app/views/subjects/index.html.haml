- @title = "Dashboard"

- unless @subjects.blank?
  - @subjects.each do |subject|
    %a.section-wrapper{ href: subject_path(subject) }
      .left
        %p.important= subject.name.capitalize
      .right
        %p= no_sessions(subject)
