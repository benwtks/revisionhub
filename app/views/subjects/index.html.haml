- @title = "Dashboard"
- @page_wrapper = true

- unless @subjects.blank?
  - @subjects.each do |subject|
    %a.section-wrapper{ href: subject_path(subject) }
      .left
        %p= subject.name
      .right
        %p 13 hours
