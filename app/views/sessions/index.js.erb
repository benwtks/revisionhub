$('.timeline-wrapper').append('<%= j render 'dates' %>')
<% if @dates.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@dates) %>')
<% else %>
    $('pagination').remove()
<% end %>
