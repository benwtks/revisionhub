- @title = "Session log"

.timeline-wrapper
- @sessions.each do |session|
  .session
    .session-details
      %p= session_date(session)
      %h2.name= session_link(session)
      = render 'tags', tags: session.topic.topicTags
