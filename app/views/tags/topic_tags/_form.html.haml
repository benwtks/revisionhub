= simple_form_for @topic_tag do |f|
  = f.input :name, label: false, placeholder: "Topic tag name"
  .form-divider
    = f.label :colour, "Tag colour:", class: "inline"
    = f.collection_select(:colour, @tag_colours, :to_s, :to_s)
  = f.button :submit
