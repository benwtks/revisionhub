= simple_form_for @topic_tag do |f|
  = f.input :name, label: false, placeholder: "Topic tag name"
  .form-divider
    = f.label :tagColour_id, "Tag colour:", class: "inline"
    = f.association :tagColour, label_method: :colour, label: false, selected: TagColour.first
  = f.button :submit
